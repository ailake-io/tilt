modelo M:
  entrada: tensor[f32, 2]
  camadas:
    - linear: [2, 2]
    - softmax
  pesos: "pesos.json"

pipeline p:
  passos:
    - a = modelo M.executar tensor([1.0, 1.0])
    - imprimir "a:", a.argmax
